 
 

export default async function Recptions() {
  // const allReceptions = await getReceptions({});

  return (
    <div className="container mx-auto py-10">
      {/* <ReceptionTable
        columns={receptionColumns} 
        data={allReceptions?.data || []}
      /> */}
    </div>
  );
}
